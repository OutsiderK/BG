# Agent Notes

本仓库采用多人/多 agent 并行开发。所有代码修改必须遵守以下边界：

- 不直接在 `main` 上开发；每个任务使用独立分支。
- 不回滚或覆盖其他 agent 的改动。
- 每个 agent 只修改任务单中声明的文件范围。
- 跨模块通信优先通过已有接口，确需改接口时先在任务单/PR 中说明。
- 设计规格以 `设定.md` 和 `spec_*.md` 为准；若规格冲突，以更具体的 `spec_*.md` 为准。
- 不提交本地缓存、导入产物、构建产物、用户配置或临时文件。

## 当前 Demo v0 技术方向

- 引擎：Godot 4.x。
- 游戏形态：2D。
- 首个目标：跑通巨木薄林单房间 demo，验证横版移动、战斗、圣痕热度、平面展开、坍缩、HUD 和死亡结算。

## 目录约定

- `project.godot`：Godot 工程入口。
- `game/scenes/`：Godot 场景。
- `game/scripts/`：GDScript 代码。
- `game/data/`：平衡表、武器、敌人、房间资源占位。
- `docs/agents/`：给各 agent 的任务单。
- `tools/`：本地校验和开发辅助脚本。

