# 平面展开机制规格 v0

> 用途：让程序员看着这份文档可以直接实现"平面展开"系统而不需要再问设计。
> 状态：v0 草案，所有数值需通过原型测试调整。
> 关联文档：[设定.md](设定.md) §7、[spec_数值基线.md](spec_数值基线.md)、[q.md](q.md)

---

## 1. 资源系统：圣痕热度

### 1.1 基础参数
| 参数 | 值 | 说明 |
|------|-----|------|
| 基础上限 | 100 点 | 达到基础上限即可发动 |
| 发动阈值 | 100 点 | 不允许半满发动 |
| 展开期间消耗速率 | -5 点/秒 | 100 热度可支撑 20 秒 |
| 局外/局内可强化 | 是 | 提升热度上限会延长展开时间；例如 150 热度可持续 30 秒 |

### 1.2 热度获取（基础值）
| 行为 | 热度 |
|------|------|
| 普攻命中敌人 | +2 |
| 击杀普通怪 | +5 |
| 击杀精英怪 | +15 |
| 击杀 Boss（一次性，触发） | +50 |
| 每次受击 | +5 |

设计意图：
- 进攻为主获取，但受伤可作为"翻盘补偿"
- 击杀奖励大于命中奖励，鼓励快速清场
- 普通战斗 ≈ 30 次命中 + 10 次击杀 = 80 点；1-2 场战斗可攒满一次展开

### 1.3 UI 表现
- HUD 显示圆环或条形热度槽，位于血量条下方
- 满 100 时槽边缘有圣纹脉冲发光提示
- 展开期间槽快速倒计（视觉上呼应消耗）

---

## 2. 发动与过渡

### 2.1 触发条件（按 1 键时检查）
1. 当前圣痕热度 >= 100
2. 当前不在展开中
3. 当前不在过渡帧（包括坍缩后的禁展冷却）
4. 当前不在反展开领域内（缝界裁判等敌人会施加该领域）
5. 当前不在过场动画 / 暂停菜单 / 死亡结算

任一条件不满足：按 1 无反应（可考虑短促错误音效）。

### 2.2 过渡阶段（总长 0.5 秒）

时间线（基于"短暂暂停 + 边缘折页"设计）：

| 时间段 | 事件 |
|--------|------|
| 0.00s | 按 1 触发，游戏世界完全冻结（time_scale = 0），敌人/弹道/计时器全部暂停 |
| 0.00 - 0.10s | 屏幕亮度从 100% 平滑降至 30%（不全黑，保留剪影） |
| 0.10 - 0.40s | 屏幕 4 条边缘出现"纸页向内折叠"特效，露出底层"厚度"质感 |
| 0.40 - 0.50s | 视角从横版抬升至俯视（45° → 90°），圣纹圆环从角色脚下扩散至全屏 |
| 0.50s | 进入展开状态，恢复 time_scale = 1，玩家可操作 |

过渡期间：
- 玩家无敌
- 玩家无法移动 / 攻击
- 不消耗热度
- 不计入 20 秒倒计时（倒计时从 0.50s 后开始）

### 2.3 音效
- 0.00s：圣痕爆鸣（低频"咚"+ 高频"嗡"叠加）
- 0.10s：纸页撕裂感
- 0.50s：环境音场拉宽，加入空间残响

---

## 3. 展开状态的玩法规则

### 3.1 操作映射变化

| 按键 | 纵平面 | 展开（俯视） |
|------|--------|------|
| W | 跳跃 | 向上移动 |
| A | 左移 | 左移 |
| S | 下落/交互 | 向下移动 |
| D | 右移 | 右移 |
| 空格 | 冲刺（最多 2 次连续，短无敌） | 同左，方向为当前移动方向 |
| 鼠标左键 | 普攻 | 普攻（攻击模式可能因武器改变） |
| Q | 武器特殊 | 武器特殊（同上） |
| 1 | 发动展开 | 主动结束展开 |
| E | 道具交互 | 道具交互 |

冲刺规则在两种状态下一致：起手 0.2s 无敌 + 位移；冷却 < 1s；可连续 2 次后短冷却。

### 3.2 空间映射

**坐标系**：纵平面是 2D 侧视（XY 平面，Y 为重力轴）；展开是 2D 俯视（XZ 平面，Z 为前后轴）。

**展开瞬间映射规则**（见 q.md B4）：
- 角色 X 坐标保留
- 角色 Y/Z 坐标 → 落到"该房间俯视地面层中心"
- 房间俯视层范围 = 房间纵平面 X 范围 × 设计师手动定义的"俯视纵深 Z 范围"（每个房间预设）

**敌人映射规则**：
- 展开瞬间，所有存活敌人的 X 坐标保留
- 敌人 Z 坐标按房间预设 lane 分配；每个房间需要定义 3-5 条俯视 lane
- 普通敌人优先分散到离玩家较远的 lane，避免展开瞬间贴脸
- 精英怪和 Boss 使用手动指定 lane 或锁定在房间中心/封印节点附近
- 若某个敌人映射点落在障碍物内，则吸附到同 lane 最近合法点

**俯视图无 Z 轴高度概念**（见 q.md B2）：
- 所有地形要么是平面障碍物（不可穿越），要么是掩体（可被攻击挡住），要么是平地
- 不允许"跳上去"
- 冲刺仍可用作位移，但不能跨越障碍物

**纵平面机关在展开期间消失**（见 q.md B7）：
- 消失的平台、激光、钟摆、坠落机关全部暂停
- 这是展开的战术价值之一：避开纵平面机关

### 3.3 弹道与残留物

**展开瞬间已存在的弹道**：全部消失（见 q.md B3）。
**展开期间产生的弹道**：使用俯视形态的弹道资源（每把武器需有两套弹道资源）。

### 3.4 敌人行为变化

- 部分敌人在展开后会有不同 AI（设定 §3.1 提到"绕树移动"等）
- 每种敌人需在 prefab 中同时定义"纵平面 AI"和"俯视 AI"两套
- 部分 Boss 在展开后会暴露弱点（封印节点）
- 反展开敌人（锚钉修士、缝界裁判、灾厄裂口）行为细节见 §6

---

## 4. 结束与坍缩

### 4.1 结束方式

| 方式 | 触发条件 | 后果 |
|------|---------|------|
| 自然结束 | 20 秒倒计时归零 或 热度耗尽 | 正常结束，剩余热度归零，禁展冷却 1.5s |
| 主动结束 | 展开中再按 1 | 正常结束，剩余热度保留，禁展冷却 1.5s |
| 受击坍缩 | 玩家受到任何伤害 | 强制结束，剩余热度归零，禁展冷却 3s |

### 4.2 坍缩动画（受击触发）

| 时间段 | 事件 |
|--------|------|
| 0.00s | 玩家受击（伤害正常结算）+ 屏幕红色闪烁 |
| 0.00 - 0.20s | 视角快速从俯视拉回横版，背景边缘合拢 |
| 0.20 - 0.30s | 圣痕短暂熄灭特效（角色身上）|
| 0.30s | 恢复纵平面玩法，进入禁展冷却 |

坍缩期间玩家保留 §A3 的 0.5s 受击无敌，可避免连续吃伤害。

### 4.3 还原规则（见 q.md B6）

| 元素 | 还原方式 |
|------|---------|
| 展开期间被击杀的敌人 | 不复活 |
| 玩家位置 | X 保留，Y 吸附到最近平台 |
| 展开后仍存活的敌人 | X 保留，Y 吸附到最近合法地面点；若附近无合法点，回到该敌人房间预设重定位点 |
| 局内货币 / 已捡道具 | 全部保留 |
| 展开期间清掉的纵平面机关 | 重新激活 |
| 弹道 | 展开期间所有弹道消失，纵平面新弹道由 AI 重新生成 |

---

## 5. 代价系统

### 5.1 彻底死亡概率增量

| 触发 | 增量 |
|------|------|
| 完整展开（自然结束） | +0.5% |
| 主动结束且未受击 | +0.2% |
| 受击坍缩 | +1% |
| 单局展开累计上限 | +4% |

注：这些数字是 q.md C3 之后的调整版，**初始概率 12% 可在 dev 设置中调整**（用户希望后期可调到 15%）。

### 5.2 灾厄侵蚀增量（仅影响本局）

| 触发 | 增量 |
|------|------|
| 完整展开 | +1 点侵蚀 |
| 主动结束 | +0.5 点 |
| 受击坍缩 | +2 点 |

阈值（见 q.md K2）：
- 0-4 点：白色（安全）
- 5-9 点：黄色（警戒，影兽出现率 +30%）
- 10+ 点：红色（影响 Boss 行为，增加额外攻击）

清除方式：
- 祭坛交易"净化锚祷"：消耗 10 灰币 -5 点
- 特定道具"净锚灰"：使用后 -3 点
- 常规情况不可降低，只能熬到本局结束

### 5.3 UI 反馈
风险变化时按 q.md I2：HUD 概率数字短暂放大 + 红色脉冲（升）/蓝色脉冲（降）+ 短音效。

---

## 6. 反展开机制

### 6.1 反展开领域
特定敌人（如缝界裁判）可在地面生成圆形领域。
- 视觉：紫色圣纹圆圈 + 内部空间扭曲
- 玩家进入：HUD"展开"图标变灰，按 1 无效
- 玩家离开：1 秒后恢复

### 6.2 反展开敌人示例

**锚钉修士**：
- 投掷锚钉前摇 1.2 秒（明显抬手 + 锚钉发光）
- 弹道可被远程武器（弓、法杖、回旋刃）打掉
- 近战需要冲刺规避
- 命中正在展开的玩家：立刻坍缩 + 玩家正常受 1 点伤害
- 命中未展开的玩家：正常受伤

**缝界裁判**：
- 站桩生成反展开领域，领域持续到自己死亡
- 自身较脆，优先击杀

**灾厄裂口**：
- 展开期间会主动靠近玩家
- 接触会快速吸取热度（-20/秒）
- 在纵平面较弱，展开后变强（HP +50%、伤害 +30%）

---

## 7. 数值参数总表（开发用）

```
[圣痕热度]
HEAT_BASE_MAX = 100
HEAT_TRIGGER_THRESHOLD = 100
HEAT_HIT = 2          # 命中
HEAT_KILL_NORMAL = 5  # 击杀普通怪
HEAT_KILL_ELITE = 15  # 击杀精英
HEAT_KILL_BOSS = 50   # 击杀 Boss
HEAT_DAMAGED = 5      # 每次受击，不按伤害量放大

[展开]
UNFOLD_DURATION = 20.0       # 秒
UNFOLD_CONSUME_RATE = 5.0    # 热度/秒
UNFOLD_TRANSITION_IN = 0.5   # 进入过渡
UNFOLD_COLLAPSE_TIME = 0.3   # 坍缩动画
UNFOLD_COOLDOWN_NORMAL = 1.5 # 正常结束后禁展冷却
UNFOLD_COOLDOWN_COLLAPSE = 3.0 # 坍缩后禁展冷却

[彻底死亡风险]
PERMADEATH_BASE = 0.12   # 12%，dev 可调
RISK_UNFOLD_FULL = 0.005
RISK_UNFOLD_EARLY = 0.002
RISK_COLLAPSE = 0.01
RISK_CAP_PER_RUN = 0.04

[灾厄侵蚀]
CORRUPTION_THRESHOLD_YELLOW = 5
CORRUPTION_THRESHOLD_RED = 10
CORRUPTION_UNFOLD_FULL = 1
CORRUPTION_UNFOLD_EARLY = 0.5
CORRUPTION_COLLAPSE = 2
```

---

## 8. 开发优先级（原型版）

按 P0/P1 实现顺序：

**P0 - 必须**：
1. 圣痕热度系统（资源累积、UI 槽）
2. 过渡动画（0.5s 黑闪+折页）
3. 视角切换（横版 → 俯视）
4. 坐标映射（角色落点）
5. 展开期间的操作映射
6. 坍缩条件检测（受击/超时/主动）
7. 还原规则
8. 彻底死亡概率累加

**P1 - 应该**：
9. 灾厄侵蚀系统
10. 反展开领域
11. 1 种反展开敌人（推荐锚钉修士）
12. UI 反馈（数字脉冲、音效）

**P2 - 可后**：
13. 多种过渡动画变体
14. 复杂反展开敌人
15. 局外天赋强化展开

---

## 9. 已知不确定项（需测试验证）

1. **20 秒展开时长是否过长**：1 区 Boss 战约 60-90s，20s 展开占 1/3，可能太强。备选：15 秒。
2. **受击立刻坍缩是否过严**：可考虑改为"受到 2 点以上伤害才坍缩"。
3. **+1% 受击坍缩惩罚是否过重**：玩家可能因为想保护风险而不展开。
4. **0.5s 过渡是否会被滥用为"无敌帧"**：极端情况下玩家可能在被打到瞬间按 1 来吃 0.5s 无敌。考虑增加发动后 0.3s 内任意受击都按坍缩处理（不享受过渡无敌）。
5. **俯视纵深 Z 范围**：每个房间手动定义是否成本太高？是否可以用统一比例（如 X 范围的 0.8x）？

这些不阻塞动手，但需要在原型测试 1-2 周后回头校准。

---

## 10. 实现建议（Godot 4 假设）

- 展开状态用单独场景树根管理（避免污染纵平面节点）
- 视角切换用 Camera2D（横版） 和 Camera2D + 俯视 shader 实现，避免 3D 相机
- 折页动画用 Shader + AnimationPlayer 控制屏幕四边的"折叠"扭曲
- 坐标映射用一个 `UnfoldMapper` 单例处理两种坐标互转
- 敌人 AI 用 `state_machine` 分 `VerticalState` / `UnfoldedState`，由外部信号切换
