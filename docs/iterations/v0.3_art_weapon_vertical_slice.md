# v0.3 美术与武器垂直切片

状态：规划完成，等待开工。

## 目标

当前 demo 已经能跑通移动、战斗、展开和 HUD，但画面仍是灰盒感，武器也只有命中盒，没有“玩家正在挥剑”的视觉事实。v0.3 的目标是先把一个房间做成可展示的垂直切片：玩家、敌人、剑、场景、HUD 和展开效果都要有统一风格，哪怕仍然是低成本资产，也要让玩家第一眼明白这是“折维圣徒”而不是工程调试场景。

本轮不追求最终商业美术，不做大规模资源外包，也不把 6 把武器全部做成完整玩法。重点是建立美术方向、资源组织方式和武器可视化框架，让后续 agent 能并行扩展。

## 美术定位

关键词：

- 二维封印世界：空间像被压成纸页，边缘有折痕、切线、错层。
- 圣徒残响：主角不是华丽骑士，而是被坐标钉住的无名执行者，轮廓克制、动作干净。
- 巨木薄林：第一房间要像一片被压薄的森林切片，树干、枝台、根墙都是“可读的地形”。
- 灾厄侵蚀：不要只用红黑污染，优先用坐标噪声、线条错位、局部透明、裂纹扩散表达。
- 武器即构筑入口：每把武器从剪影、攻击轨迹和 UI 图标上就要能区分玩法倾向。

视觉优先级：

1. 可读性高于细节。玩家、敌人、武器、危险预警、可站立地面必须在 720p 下清楚。
2. 动作反馈高于静态精度。挥剑轨迹、命中火花、受击闪烁比精修立绘更重要。
3. 可复用高于一次性装饰。先用 Godot 原生形状、简单贴图和参数化 FX，后续再替换为正式 sprite。

## 画面规格

- 目标分辨率：`1280x720`。
- 摄像机基准：横版战斗时玩家位于画面左中到中部，展开俯视时房间结构必须完整可读。
- 碰撞可视约束：所有可站立平台必须有明确上沿；所有不可通过树干/根墙必须比背景更实。
- 灰盒替换比例：v0.3 完成后，主场景中不应再出现裸 `ColorRect` 方块承担角色、敌人或核心地形表现。
- 色彩结构：
  - 背景：低饱和冷绿、深青、雾灰，负责空间深度。
  - 可行走地形：树皮棕、苔绿、浅边缘光，负责交互可读性。
  - 主角：冷白、暗蓝灰、少量圣痕金，负责从背景中跳出。
  - 敌人：腐绿、暗红、断裂白线，负责危险识别。
  - 展开/锚率：青白坐标线、金色锚点、紫红侵蚀只作为风险强调。

## 资产清单

### 主角

v0.3 先做低成本分层角色，不要求完整逐帧动画。

- `Body`：斗篷/长衣主体，竖向轮廓比当前方块更接近人形。
- `Head`：无脸面罩或头巾，避免需要五官细节。
- `ArmWeaponAnchor`：武器挂点，跟随朝向和攻击状态旋转。
- `Shadow`：地面影子，展开/坍缩时可缩放。
- 状态表现：
  - idle：轻微上下浮动或斗篷呼吸。
  - run：身体倾斜 + 脚部/衣摆摆动。
  - jump/fall：影子和身体分离感。
  - dash：残影或短线拖尾。
  - hurt：白闪/红闪 + 短暂后仰。
  - dead：去饱和、跪倒或压扁为坐标碎片。

### 武器

首批武器仍按原设定保留 6 把，但 v0.3 的落地层级分三档：

| 武器 | v0.3 表现目标 | 后续玩法定位 |
| --- | --- | --- |
| 剑 | 必须可见、可挥动、有攻击弧、有命中特效 | 近战基础、冲刺连段、稳定清怪 |
| 矛 | 有图标和地面展示占位，可作为后续武器资源样例 | 长距离直刺、穿透、站位控制 |
| 弓 | 有图标和地面展示占位 | 远程蓄力、暴击、弱点标记 |
| 法杖 | 有图标和地面展示占位 | 术式弹幕、区域控制、展开收益 |
| 回旋刃 | 有图标和地面展示占位 | 返回轨迹、背击、空间重映射 |
| 圣铃 | 有图标和地面展示占位 | 召唤、反制、热度/锚率交互 |

剑的 v0.3 必做项：

- 玩家身上有清楚的剑模型或贴图，不再只有攻击盒。
- 攻击时剑从待机角度扫过目标方向。
- 攻击弧和命中盒方向一致，避免“看起来没打到但实际打到”。
- 命中时出现短促火花、伤害数字和轻微屏幕震动。
- 展开状态下剑的朝向支持八方向或至少跟随输入方向。

武器框架建议：

- 新增 `game/scenes/weapons/WeaponVisual.tscn` 或分武器场景。
- 新增 `game/data/weapons/*.cfg` 的视觉字段：`display_name`、`icon_key`、`accent_color`、`trail_color`、`visual_scene`。
- 玩家场景保留 `WeaponAnchor`，攻击逻辑可以暂时仍调用现有 `SwordAttack`。
- v0.3 不做武器切换 UI，但数据和美术命名要允许后续扩展。

### 敌人

现有假人敌人要从调试块升级为“薄林腐化物”：

- 轮廓：低矮、前倾、像树皮/兽形碎片拼成。
- 攻击预警：地面扇形/短线闪烁，不只靠颜色块。
- 受击：裂纹亮起、短暂压缩、碎屑飞出。
- 死亡：碎成纸片/树屑，保留 0.4 秒后淡出。

### 巨木薄林房间

场景要分层：

- 远景：薄雾、树影、被压平的枝叶，不参与碰撞。
- 中景：主要树干和枝台，和碰撞平台严格对齐。
- 前景：根须、叶片、遮挡边缘，透明度低，不能挡住战斗。
- 出口/撤离占位：先做成“坐标门”或“树皮裂隙”，为 v0.4 单局流程预留。
- 展开可读性：lane 线要融入美术，像切片坐标线，而不是纯调试线。

### HUD 与 UI

v0.3 只做战斗 HUD 的美术统一，不做完整菜单。

- HP：用短条或圣痕裂纹表示，不再像纯文本调试值。
- 热度：保留数字，但加环形/槽形表现，满时给 `1` 展开按钮提示。
- 锚率/侵蚀：颜色变化和轻微抖动要明确，但不能盖住战斗。
- 武器显示：左下或右下显示当前武器“剑”的小图标和攻击冷却反馈。
- 结算/商店等界面留到 v0.4 以后。

### FX

最小 FX 包：

- 剑挥砍弧：0.10-0.18 秒，淡入快、淡出快。
- 命中火花：方向跟随击退方向，持续 0.20-0.35 秒。
- 受击碎屑：小粒子或短线，不使用昂贵粒子数量。
- 冲刺残影：1-3 个半透明残影或线条。
- 展开边缘：过渡 overlay 已有，v0.3 需要把颜色和房间 lane 统一。

## 工程落地方案

目录建议：

```text
game/art/
  README.md
  palette/
  sprites/
    player/
    enemies/
    weapons/
    room_forest/
  ui/
game/scenes/weapons/
game/scripts/weapons/
```

实现原则：

- v0.3 可以先用 `Polygon2D`、`Line2D`、`Sprite2D` 和少量贴图混合，不强制导入最终 PNG。
- 核心视觉节点命名稳定，后续替换资源时不改玩法脚本。
- 武器攻击逻辑和武器视觉分离：逻辑管命中，视觉管轨迹和挂点。
- 所有新增视觉效果要有开关或生命周期，避免节点越积越多。
- 任何影响碰撞的地形美术必须对齐已有 `StaticBody2D`，不在美术节点里偷偷改规则。

## Agent 切分建议

下一轮可以并行 5 个 agent，写入独立分支和工作树：

| Agent | 分支建议 | 负责范围 | 主要文件 |
| --- | --- | --- | --- |
| A | `feat/v0-3-art-direction` | 美术规范、调色板、目录、资源命名 | `docs/iterations/v0.3_art_weapon_vertical_slice.md`、`game/art/README.md` |
| B | `feat/v0-3-player-weapon-visual` | 主角分层视觉、武器挂点、剑可视化 | `game/scenes/player/Player.tscn`、`game/scenes/combat/SwordAttack.tscn`、`game/scenes/weapons/*`、`game/scripts/weapons/*` |
| C | `feat/v0-3-room-art-pass` | 巨木薄林房间灰盒替换、地形分层、出口占位美术 | `game/scenes/rooms/DemoRoom.tscn`、`game/art/sprites/room_forest/*` |
| D | `feat/v0-3-enemy-fx-visual` | 敌人轮廓、预警、受击/死亡 FX | `game/scenes/enemies/DummyEnemy.tscn`、`game/scripts/enemies/enemy_base.gd`、`game/scenes/fx/*` |
| E | `feat/v0-3-hud-art-pass` | HUD 美术化、武器图标槽、展开/锚率反馈统一 | `game/scenes/ui/HUD.tscn`、`game/scripts/ui/hud.gd`、`game/art/ui/*` |

冲突提醒：

- B 和 E 都可能需要当前武器信息，优先由 B 暴露只读接口，E 不改玩家攻击逻辑。
- C 不改房间清场/撤离逻辑，那是 v0.4 的范围。
- D 可以增强敌人视觉和 FX，但不改敌人 AI 数值。

## 验收标准

必须通过：

- `python3 tools/verify_scaffold.py`
- `godot4 --headless --path . --quit`
- 已有 smoke tests 不因纯美术改动失败。

人工验收：

- 打开主场景后，第一眼能看出主角、敌人、地形、武器和 HUD 分属不同信息层。
- 鼠标左键攻击时能看到剑和挥砍弧，命中位置与伤害反馈一致。
- 按 `1` 展开后，lane/落点提示不再像临时调试线，而像世界规则的一部分。
- 玩家受击、敌人受击、冲刺、坍缩都有短反馈。
- 1280x720 截图中没有角色/武器/HUD 文本明显重叠。

暂不验收：

- 6 把武器的完整战斗机制。
- 正式逐帧动画。
- 完整音效。
- 结算、商店、道具选择界面。

## 后续衔接

v0.3 完成后再进入单局流程。原因是撤离点、结算界面和道具选择都会依赖基础 UI 风格；先把主场景视觉语言定住，后续新增界面才不会继续堆调试块。
